# AI 协作工作流说明

> 本文件记录了本项目是如何利用 GitHub Copilot Agent 团队进行协作开发的。
> 适合想将 AI-Native 工作流引入自己项目的开发者参考。
>
> **最后更新：** 2026-02-25（反映 V1.x 迭代实际工作流）

---

## 核心理念

**传统开发流程：** 人类 → 写代码 → 审查 → 提交

**AI-Native 工作流（本项目采用）：**
```
人类（目标 & 最终决策）
    ↓
主 Agent（协调中枢）────── 并行调度
    ├─ profile-designer（视觉规划）
    ├─ researcher（技术调研）
    ├─ content-writer（代码实现）
    └─ qa-reviewer（质量审查）
    ↓
主 Agent（综合意见 → 执行）
    ↓
人类（确认 & 反馈）
```

人类只需要：**给目标、做决策、最终审核**。Agent 拥有技术路径优先决定权。

---

## 实际工作模式

### 模式一：团队全体会议（每迭代周期前）

主 Agent 并行召集四位 Agent，各自从专业维度输出意见：

```
主 Agent → 并行分发议题
    ├─ profile-designer：视觉评分 + 改进方向
    ├─ researcher：技术调研 + 方案推荐
    ├─ content-writer：内容评估 + 文案建议
    └─ qa-reviewer：质量审查 + 问题清单
         ↓
主 Agent：综合 4 份报告 → 生成迭代计划 → 向用户汇报
```

**会议纪要存档：** `docs/meetings/YYYY-MM-DD-会议名.md`

### 模式二：流水线执行（确认方案后）

```
designer（方案） → writer（实现） → reviewer（审查） → 推送
```

### 模式三：快速修复（小改动）

主 Agent 直接修改 + 推送，跳过完整流程。

---

## Agent 团队

### profile-designer
- **职责：** 视觉架构师，规划整体布局和组件选型
- **权限：** 只读 + 技术路径优先决定权
- **工具：** fetch（调研参考主页）、search（查找组件）、codebase
- **输出：** 设计方案文档，不直接修改 README
- **启用场景：** 每次大改版前、团队会议

### content-writer
- **职责：** 内容工程师，将设计方案转化为 Markdown 代码
- **权限：** 读写
- **工具：** editFiles（修改文件）、fetch、codebase、search
- **输出：** 实际的文件变更
- **启用场景：** 设计方案确认后

### researcher
- **职责：** 情报官，深度调研节省主会话 context
- **权限：** 只读
- **工具：** fetch、search、codebase
- **输出：** 浓缩的调研摘要（≤ 500 字关键结论 + 详细附录）
- **启用场景：** 调研新组件、分析竞品主页、评估技术方案

### qa-reviewer
- **职责：** 质检官，六维度审查质量
- **权限：** 只读 + 诊断
- **工具：** codebase、fetch、search、problems
- **输出：** 结构化审查报告（🔴 阻断 / 🟡 警告 / 🟢 建议）
- **启用场景：** 每次迭代完成后、团队会议

---

## 实际工作流示例

### 示例一：迭代二全体会议 → 执行

```
1. [你] "可以继续迭代"

2. [主 Agent] 并行召集四位 Agent 开会：
   - designer → 视觉评分 7.2/10 + 3 个改进方向
   - researcher → WakaTime 方案 + 3 个新数据源
   - writer → 内容缺口分析 + 项目文案建议
   - reviewer → 🔴 阻断问题（pin 卡片语法错误）

3. [主 Agent] 综合报告 → 生成 P1-P4 行动计划

4. [你] "团队可以开始后续工作"

5. [主 Agent] 并行执行：
   - 重写 Profile README（统一主题 + WakaTime + Activity Graph + 蛇）
   - 创建两个 GitHub Actions workflow
   - 更新 Astro 站点（About 文案 + Projects 数据 + 强调色）

6. [主 Agent] 一次性提交推送到 3 个仓库
```

### 示例二：快速修复

```
1. [你] "文档没有跟上开发进度"

2. [主 Agent] 审计所有文件 → 直接修复 → 推送
   （无需召集团队，属于维护性工作）
```

---

## 铁律：文档跟随开发

每次迭代完成后，**必须同步更新**以下文件：

| 文件 | 更新内容 |
|------|---------|
| `CHANGELOG.md` | 本次新增/变更/修复 |
| `.github/copilot-instructions.md` → 已决定的设计选择 | 新决策 |
| `.github/copilot-instructions.md` → 当前迭代状态 | 进度 |
| `docs/design-decisions.md` | 每个决策的 what/why/when |
| `docs/component-guide.md` | 新增组件的参数和用法 |
| `docs/meetings/` | 会议纪要（如有） |

**这不是可选步骤，而是迭代的收尾条件。**

---

## 配置文件说明

| 文件 | 作用 |
|------|------|
| `.github/copilot-instructions.md` | 全局项目指令，每次会话自动生效（项目大脑） |
| `.github/agents/*.agent.md` | 各 Agent 的角色定义、工具权限、handoff 规则 |
| `.vscode/toolsets.jsonc` | 工具集分组（readonly/writer/runner） |
| `.vscode/mcp.json` | MCP 服务器连接（GitHub API、fetch）|
| `.vscode/settings.json` | 终端自动批准规则 |

---

## 如何复用本工作流

1. Fork 本仓库
2. 修改 `.github/copilot-instructions.md` 中的个人信息
3. 按需调整 `.github/agents/` 中的 Agent 配置
4. 在 VS Code 中使用 GitHub Copilot Chat（Agent 模式），即可使用同一套工作流

> 详细组件说明见 [docs/component-guide.md](component-guide.md)
