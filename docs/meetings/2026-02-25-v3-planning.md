# 会议纪要 #004 — V3.0 全员规划会

**日期：** 2026-02-25  
**类型：** 里程碑规划会（Major 版本前）  
**主持：** brain  
**参与者：** brain · pm · dev · researcher · code-reviewer · profile-designer  
**前置条件：** v2.0.0 已打 tag 并推送 ✅

---

## 会议背景

V2.0 目标「从组件陈列室进化为有叙事节奏的个人品牌页」已完全达成，全部四个 Phase 交付，Agent 团队也完成了 V2.0 架构重构。本次会议目标：评估 V2.0 成果，确定 V3.0 方向，制定分阶段执行计划。

---

## 一、各部门汇报

### 🧠 brain（战略协调）

**V2.0 交付质量评估：**
- 三个仓库均已推送，CI 绿色，无 404 链接
- Agent 团队 V2.0 重构完成，team-playbook 落地
- **主要遗留问题：** Astro 博客尚无标签/分类系统；Profile README 暂无 GitHub Achievements 展示

**V3.0 战略判断：**
> V3.0 应完成从「个人品牌页」到「可被发现的内容平台」的跃迁。
> 当前 V2.0 是打好了地基；V3.0 要装修、引流、形成内容生态。

**3 个核心目标：**
1. **博客成熟化** — 从「能用」升级到「值得收藏」（RSS + 标签 + SEO）
2. **Profile 深度化** — 数据更丰富，互动入口更清晰
3. **模板社区化** — OpenProfile 从「个人使用」升级到「社区可复用」

---

### 📋 pm（项目经理）

**V2.0 Sprint 回顾：**
- 规划项 34 个，交付 34 个，0 遗留（完成率 100%）
- 最大风险：@astrojs/mdx 版本冲突，通过 researcher 调研快速解决
- 文档跟随率：本次达到 100%（design-decisions + component-guide + team-playbook 全同步）

**V3.0 工作量预估：**

| 模块 | 复杂度 | 预计任务数 |
|------|--------|-----------|
| Blog SEO 体系 | 中 | 8 |
| Blog 标签/分类 | 低 | 5 |
| RSS Feed | 低 | 3 |
| Profile Achievements | 低 | 4 |
| Profile 3D 贡献图 | 低 | 2 |
| OpenProfile 一键配置脚本 | 高 | 8 |
| Astro OpenGraph 图片 | 高 | 6 |
| Agent MCP 深度集成 | 高 | 6 |
| **合计** | | **~42** |

**建议分期策略：**
- **V3.0（P0 核心）：** Blog SEO + RSS + 标签 + Profile 新组件
- **V3.1（P1 增强）：** OpenGraph 自动生成 + 一键配置脚本
- **V3.2（P2 社区）：** Agent MCP 深度集成 + 社区模板文档

**推荐 V3.0 DoD：**
```
✅ RSS Feed 可订阅（/rss.xml 可访问）
✅ Blog 标签系统可用（/blog/tags/[tag] 路由）
✅ Sitemap.xml 自动生成
✅ Profile README 展示 GitHub Achievements
✅ Profile README 新增 3D 贡献图（可选/折叠）
✅ CHANGELOG [3.0.0] 完整
✅ code-reviewer APPROVED
✅ Lighthouse SEO 分数 ≥ 90（Astro 站点）
```

---

### 💻 dev（全栈实现）

**技术可行性分析：**

**RSS Feed — 绿灯 ✅**
```
方案：@astrojs/rss（官方包，Astro 4.x 兼容）
实现：src/pages/rss.xml.ts
复杂度：低，2 小时内完成
```

**Blog 标签系统 — 绿灯 ✅**
```
方案：利用现有 Content Collections schema 扩展 tags 字段
路由：src/pages/blog/tags/index.astro + [tag].astro
复杂度：低-中，Astro 原生支持
```

**Sitemap — 绿灯 ✅**
```
方案：@astrojs/sitemap（官方包）
配置：在 astro.config.mjs 中 site 字段 + integration
复杂度：极低，30 分钟配置
```

**OpenGraph 图片自动生成 — 黄灯 ⚠️**
```
方案 A：Satori（@vercel/og）—— Astro 4.x 支持良好
方案 B：手动为每篇文章配置 og:image 静态图
建议：V3.1 再做，V3.0 先用 fallback 静态图
```

**GitHub Achievements — 绿灯 ✅**
```
方案：github-profile-trophy + GitHub Profile Summary Cards
复杂度：低，已有现成 badge URL
```

**3D 贡献图 — 绿灯 ✅**
```
方案：Platane/skyline-problem or Ashutosh00710/github-readme-activity-graph 3D view
建议：使用 github-profile-3d-contrib（仅需 Action + SVG embed）
```

**实现顺序建议（流水线）：**
```
1. Sitemap（@astrojs/sitemap）          ← 30m
2. RSS（@astrojs/rss）                  ← 2h
3. Blog 标签系统（路由 + UI）            ← 3h
4. Profile: Achievements + 3D 贡献图    ← 2h
5. SEO meta 补全（每页 title/desc/og）  ← 2h
```

---

### 🔍 researcher（技术调研）

**调研一：Astro 4.x RSS 最佳实践**

`@astrojs/rss@4.0.9` 与 Astro 4.16.18 完全兼容。API：

```typescript
// src/pages/rss.xml.ts
import rss from '@astrojs/rss';
import { getCollection } from 'astro:content';

export async function GET(context) {
  const posts = await getCollection('blog');
  return rss({
    title: 'Ray Huang · Blog',
    description: 'AI-Native 开发实践 & LLM 工程思考',
    site: context.site,
    items: posts.map(post => ({
      title: post.data.title,
      pubDate: post.data.pubDate,
      description: post.data.description,
      link: `/blog/${post.slug}/`,
    })),
  });
}
```

**调研二：GitHub Achievements 展示组件**

推荐方案：`github-profile-trophy`

```markdown
[![Trophy](https://github-profile-trophy.vercel.app/?username=njueeRay&theme=darkhub&no-frame=true&row=1&column=6)](https://github.com/ryo-ma/github-profile-trophy)
```

- 支持 `darkhub` 主题（与品牌色一致）
- `row=1&column=6` 紧凑横排
- 已验证 njueeRay 账号可正常渲染（有足够 Achievement 数据）

**调研三：3D 贡献图**

推荐：`yoshi389111/github-profile-3d-contrib`  
- GitHub Action，每日自动更新
- 输出：`profile-3d-contrib/profile-night-rainbow.svg`（暗色主题）
- 与现有贡献蛇 Action 共存无冲突

**调研四：Sitemap 配置**

```javascript
// astro.config.mjs 追加
import sitemap from '@astrojs/sitemap';
export default defineConfig({
  site: 'https://njueeray.github.io',
  integrations: [mdx(), sitemap()],
});
```

---

### 🔎 code-reviewer（质量审查）

**V2.0 发布后健康度评估：**

| 维度 | 得分 | 说明 |
|------|------|------|
| 正确性 | 9/10 | 所有组件正常渲染，1 个潜在问题见下 |
| 安全性 | 10/10 | 无外部脚本注入，无敏感信息 |
| 性能 | 7/10 | Astro 站点缺少 sitemap 和 OG 图片 |
| 可维护性 | 9/10 | Content Collections 架构良好，team-playbook 到位 |
| 规范性 | 9/10 | markdownlint 通过，commit 规范符合 |
| 兼容性 | 8/10 | 双模组件已接入，待验证移动端 |
| 文档完整性 | 10/10 | 所有变更均有文档记录 |

**整体评级：** `APPROVED_WITH_SUGGESTIONS`

**发现问题（按优先级）：**

🟡 **P1 — SEO 缺失**  
Astro 各页面没有独立的 `<meta name="description">` 和 `og:title/og:description`。  
修复：在 `BaseLayout.astro` 增加 props 接收，`PageMeta` 组件统一注入。

🟡 **P1 — Blog 缺少 404 处理**  
访问不存在的 `/blog/xxx` 会触发 Astro 默认 404，建议添加 `src/pages/404.astro`。

🟢 **P2 — RSS 链接需要放置**  
上线 RSS 后要在 `<head>` 注入 `<link rel="alternate" type="application/rss+xml">`，  
同时在 Footer 添加 RSS 图标入口。

🟢 **P3 — 3D 贡献图加载速度**  
SVG 文件较大（约 300KB），建议放在 `<details>` 折叠区块内，与 WakaTime 同级。

---

### 🎨 profile-designer（视觉规划）

**V2.0 视觉验收：**  
双模兼容完整，分隔线层次感良好，Stats/Streak 并排比例 60/40 视觉协调。  
**评分：** 视觉完成度 8.5/10（+2 分对比 V1.x）

**V3.0 视觉方向建议：**

**建议 1 — Profile README 新增「成就」区块**  
位置：WakaTime 折叠区块与 Featured Projects 之间  
布局：trophy 横排 6 个，3D 贡献图放在 `<details>` 内  
配色：darkhub 主题与 #0d1117 背景无缝衔接

**建议 2 — Astro Blog 标签视觉系统**  
标签使用 `#58a6ff` 细描边 pill 样式，hover 时填充  
标签云页面：按使用频次排列，字号渐变（1 篇 → 5 篇）

**建议 3 — Astro 站点 SEO 卡片**  
OG 图片统一底色 `#0d1117`，左侧 accent bar `#58a6ff`，右侧文章标题 + 站点 logo  
V3.0 用静态图，V3.1 用 Satori 自动生成

---

## 二、V3.0 综合决策

### 决策 A — V3.0 范围（已确认）

```
【纳入 V3.0 核心】
✅ RSS Feed（@astrojs/rss）
✅ Blog 标签/分类系统（路由 + UI）
✅ Sitemap（@astrojs/sitemap）
✅ SEO meta 补全（BaseLayout props + PageMeta）
✅ 404 页面（src/pages/404.astro）
✅ Profile README：github-profile-trophy（darkhub 横排）
✅ Profile README：3D 贡献图（yoshi389111，折叠放置）
✅ Profile README 添加 RSS 订阅入口 badge

【推迟至 V3.1】
⏳ OpenGraph 自动生成（Satori，复杂度高）
⏳ 一键配置脚本（CLI 工具）

【推迟至 V3.2】
⏳ Agent MCP 深度集成（GitHub API 直调）
⏳ 社区模板 showcase 页面
```

### 决策 B — 分阶段执行计划

**Phase X（Astro 站点 SEO 地基）**
```
X-1: 安装 @astrojs/sitemap，配置 site URL
X-2: 安装 @astrojs/rss，创建 src/pages/rss.xml.ts
X-3: BaseLayout.astro 增加 PageMeta props（title/description/og）
X-4: 所有页面（index/about/projects/blog/[slug]）传入正确 meta
X-5: 添加 src/pages/404.astro（终端 404 风格）
```

**Phase Y（Blog 标签系统）**
```
Y-1: content/config.ts 的 blog schema 扩展 tags 字段（已有，确认类型）
Y-2: src/pages/blog/tags/index.astro（标签云）
Y-3: src/pages/blog/tags/[tag].astro（按标签过滤）
Y-4: blog/index.astro 添加标签筛选 UI
Y-5: [slug].astro 文章页添加标签 pill 显示
```

**Phase Z（Profile README 新组件）**
```
Z-1: 添加 github-profile-3d-contrib Action（每日更新 SVG）
Z-2: Profile README 插入 trophy 区块（achievements 区）
Z-3: Profile README 插入 3D 贡献图（<details> 折叠）
Z-4: Profile README 页脚添加 RSS 订阅 badge
```

### 决策 C — 语言偏好记录

用户确认：**所有撰写内容（博客、会议纪要、文档）默认使用中文**。  
技术术语（类名、文件名、CLI 命令）保持英文。  
copilot-instructions.md 已记录此偏好，Agent 在生成内容时默认遵守。

---

## 三、V3.0 DoD 清单（pm 锁定）

在 code-reviewer 输出 `APPROVED` 之前，以下项全部必须打 ✅：

- [ ] `https://njueeray.github.io/rss.xml` 返回有效 XML
- [ ] `https://njueeray.github.io/sitemap-index.xml` 存在且包含所有页面
- [ ] `/blog/tags/` 页面可访问，展示所有标签
- [ ] 点击标签可过滤文章列表
- [ ] 每篇博文有独立 og:title 和 og:description
- [ ] 访问 `/404` 返回自定义 404 页面
- [ ] Profile README 展示 trophy 组件（darkhub 主题）
- [ ] Profile README 3D 贡献图（`<details>` 折叠）已上线
- [ ] Profile README 有 RSS 订阅入口
- [ ] `CHANGELOG.md [3.0.0]` 完整填写
- [ ] Lighthouse SEO 分数 ≥ 90（Astro 站点首页）
- [ ] code-reviewer 输出 `APPROVED`
- [ ] v3.0.0 tag 已推送到 OpenProfile

---

## 四、Action Items

| 负责人 | 任务 | 优先级 |
|--------|------|--------|
| dev | Phase X（SEO 地基 5 项） | P0 |
| dev | Phase Y（标签系统 5 项） | P0 |
| dev | Phase Z（Profile 新组件 4 项） | P1 |
| dev | 博客现有 2 篇补充 tags 字段 | P1 |
| pm | V3.0 DoD 清单跟进 | 持续 |
| code-reviewer | V3.0 上线前全项审查 | V3.0 收尾 |
| brain | 会后更新 copilot-instructions.md 当前状态 | 立即 |

---

## 五、会议决议摘要

1. **V3.0 核心：** Blog 成熟化（RSS + 标签 + SEO）+ Profile 成就展示
2. **语言规范：** 所有原创内容默认中文，技术符号保持英文
3. **V3.0 预计规模：** ~25 个任务，分 X/Y/Z 三个 Phase
4. **下一步：** brain 确认用户授权后，dev 立即推进 Phase X

> **会议关闭时间：** 2026-02-25 | 记录人：brain
