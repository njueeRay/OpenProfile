# 团队复盘会议 #003 — AI 协作范式深化与核心资产沉淀

**日期：** 2026-02-25  
**参与者：** brain（新设）· profile-designer · content-writer · researcher · qa-reviewer · pm（新设）  
**会议模式：** 全体复盘会议（并行发言 → 主 Agent 综合）  
**触发：** 用户提议 — 沉淀团队核心资产，为未来跨项目可复用做准备  
**状态：** ✅ 决议形成，已执行配置更新

---

## 一、复盘背景

三个子项目历经两次全体会议、多轮迭代：

| 里程碑 | 日期 | 结果 |
|--------|------|------|
| 会议 #001 架构启动 | 2026-02-25 | 三仓库架构确立，V1.x 蓝图 |
| V1.x 迭代完成 | 2026-02-25 | Profile README + Astro 站点 + 文档体系 |
| 会议 #002 V2.0 规划 | 2026-02-25 | 30+ 任务分四 Phase 执行 |
| V2.0 Phase A/B/C/D 完成 | 2026-02-25 | 本次复盘时间点 |

---

## 二、各角色复盘发言

### 🧠 主 Agent（协调中枢）

> **三轮协作最深的感受：角色边界模糊是最大的效率损耗点。**

本次协作中，"主 Agent"同时承担了策略规划、任务分发、代码实现、文档更新、Git 操作、用户汇报六个职责。这在单会话 context 下勉强可行，但存在严重隐患：

1. **单点失败**：主 Agent 疲于执行时，策略质量下降，容易遗漏 QG 门禁
2. **角色混淆**：用户无法清楚知道当前 Agent 在"规划"还是"执行"
3. **无人管项目**：没有专属 PM 角色，任务状态仅靠会议纪要追踪，跨会话后状态易丢失

**提案：** 引入 `brain` 和 `pm` 两个正式角色，构建五角色标准团队。

---

### 🎨 profile-designer

> **视觉决策没问题，但"视觉"这个定语让我无法被其他项目复用。**

我的核心能力是：**信息架构规划 + 技术组件选型 + 视觉语言体系建立**。这套能力不只适用于 Profile README，也适用于 Landing Page、文档站、CLI 工具的 README。

建议把我泛化为 `architect`（架构师）或保持 `designer`，但职责描述脱离"Profile"场景，改为通用的 UI/UX + 信息架构工作。

**本轮亮点：** V2.0 区块顺序重组方案从"组件列表"变为"叙事流"，这是有效的。  
**本轮盲点：** 应在 Phase A 规划时就输出"移动端 Markdown 渲染兼容"检查清单，而非事后 QA 发现。

---

### ✍️ content-writer

> **我是执行层，执行质量取决于上游规划的颗粒度。**

本轮收到的任务最细的是 Phase A（每条任务单独列出，有预估时间），这时候我输出质量最高。Phase C 的"添加顶部导航栏"任务没有 wireframe，我自己设计了终端风格——运气好，用户接受了。但这不是可靠的流程。

**改进建议：**
1. `designer` 输出方案时，HTML 结构草图应作为必交付物，不能只有文字描述
2. 引入 `dev` 泛化名称，我不应该只写 Markdown，应该能处理 TypeScript/Python/YAML/任何语言的实现
3. 每次实现前要有 **实现计划（Implementation Plan）**，明确修改哪些文件的哪些部分

---

### 🔍 researcher

> **我的价值在于降低主会话 context 消耗，但我被调用得太少。**

本轮迭代中，我只在规划阶段被提及（WakaTime 方案、Astro 升级评估），执行阶段几乎没有调用我。实际上有多处可以提前让我调研：

- Astro 4.x 的 `@astrojs/mdx` 兼容版本（Phase C 执行时才发现 mdx@4 不兼容，浪费了一次 install 失败）
- `komarev` 访客计数器 vs `shields.io/endpoint` 方案对比
- lychee-action 的 `--exclude` 参数语法

**核心建议：** 在每个 Phase 执行前，由 PM 触发一次 researcher 预调研（pre-flight research），把"执行时才会发现的坑"提前暴露。

---

### ✅ qa-reviewer

> **我是守门人，但门在 Phase D 才建起来——这是根本性问题。**

V1.x 整个迭代中，CI 完全缺失。所有质量依赖我的手动审查报告，而手动审查只在迭代末尾发生。这意味着：

- 链接有效性：靠人眼检查
- Markdown 格式：靠经验判断
- 浅色主题兼容：V2.0 之前完全没检查

**三个铁律建议：**
1. **CI 先行**：任何新项目第一个 commit 后，第二个 commit 就要建立基础 CI（lint + link-check）
2. **QG 门禁自动化**：能用 Action 检查的，不依赖手动 QA
3. **我的角色泛化**：`qa-reviewer` → `code-reviewer`，职责扩展到所有语言的代码质量，不仅限于 Markdown

---

## 三、用户抛砖引玉 — 分析与响应

用户提出了四个核心观点，团队逐条回应：

### 观点 1：构建顶尖团队，包含 Brain / Dev / PM / CodeViewer

✅ **全员认同**。重构如下：

```
旧团队（项目特化）              新团队（通用框架）
─────────────────────────────────────────────────────
主 Agent（无明确定义）     →   brain（战略大脑，用户唯一汇报点）
profile-designer           →   designer / architect（信息架构，方案输出）
content-writer             →   dev（全栈实现，任何语言）
researcher                 →   researcher（保留，通用）
qa-reviewer                →   code-reviewer（质量门，泛化）
（缺失）                   →   pm（项目管理，任务追踪，版本发布）
```

### 观点 2：重要节点召开全体会议

✅ **已有实践，需规范化**。会议类型化：

| 类型 | 触发时机 | 参与 Agent |
|------|---------|-----------|
| 架构会议 | 新项目启动 | 全体 |
| Sprint 规划会议 | 每个迭代周期前 | brain + pm + 相关 dev |
| 复盘会议 | 里程碑完成后 | 全体 |
| 紧急热修复 | 阻断性 bug | brain + dev + code-reviewer |

### 观点 3：核心配置、文档动态跟随，建议沉淀为核心资产

✅ **已有 DoD Checklist，但执行率不稳定**。根因：DoD 检查依赖"人记得做"，没有 CI 强制触发。

改进：
- `copilot-instructions.md` 作为**项目活文档**，每次会话开始由 brain 先读，每次会话结束由 pm 验证更新
- `docs/team-playbook.md` 新建为**永久性团队手册**，跨项目可直接带走

### 观点 4：Commit 遵循开源规范，包括何时 Release

✅ **V1.x 已遵循语义化 commit，但 Release 时机不明确**。制定清晰规则见下节。

---

## 四、核心决议

### 决议 A — 标准团队角色体系（通用框架）

```
┌─────────────────────────────────────────────────────┐
│                      brain                          │
│   战略规划 · 团队协调 · 用户唯一汇报点 · 优先决定权   │
└──────────┬──────────────────────────────────────────┘
           │ 分发
     ┌─────┼──────────────────────────────────┐
     ▼     ▼              ▼                   ▼
    pm    dev         researcher         code-reviewer
  项目管  全栈实现     技术调研             质量门禁
  任务追踪 任何语言    方案分析             代码审查
  版本发布 文件读写    浓缩摘要            阻断问题上报
  DoD执行              不修改文件
```

**跨项目使用方式：** 这五个角色是通用底座。特定项目可叠加专项角色（如本项目的 `profile-designer`，后续 Python 项目可叠加 `arch-designer`）。

### 决议 B — Commit 规范 + Release 时机

**Commit 类型**（Angular 约定扩展版）：

| 类型 | 场景 | 示例 |
|------|------|------|
| `feat` | 新功能、新组件 | `feat(blog): add Content Collections + 2 posts` |
| `fix` | Bug 修复 | `fix(astro): downgrade mdx to v3 for Astro 4.x compat` |
| `docs` | 文档变更 | `docs: sync design-decisions for V2.0` |
| `style` | 格式、无逻辑变更 | `style(readme): align JSON fields` |
| `refactor` | 重构、无新功能 | `refactor(readme): restructure block order` |
| `chore` | 构建、配置、依赖 | `chore: add .gitattributes enforce LF` |
| `ci` | CI/CD 变更 | `ci: add link-check + markdown-lint actions` |
| `perf` | 性能优化 | `perf(astro): lazy load hero image` |
| `test` | 测试 | `test: add e2e build smoke test` |

**Scope 规范**（括号内，可选）：  
项目代号 `readme` / `astro` / `ci` / `docs` / `agent` / `deps`

**Release 时机**（由 pm 触发）：

| 条件 | 版本类型 | 操作 |
|------|---------|------|
| Phase / 里程碑完成，无 Breaking Change | `vX.Y.0` Patch Release | `git tag -a + GitHub Release + CHANGELOG 拆分` |
| 重大功能完成（多 Phase 合并） | `vX.0.0` Minor Release | 同上 + Release Notes 详细说明 |
| Breaking Change（架构重构） | `vX+1.0.0` Major Release | 需用户确认 + Migration Guide |

### 决议 C — 会话连续性（Session Continuity）协议

> **问题根因：每次新 Copilot 会话 context 清空，团队"失忆"**

**标准开场协议（brain 执行）：**
```
1. 读取 .github/copilot-instructions.md → 确认当前项目状态
2. 读取 docs/meetings/ 最新纪要 → 了解上次结束状态
3. 输出简报：[项目状态] + [待执行任务] + [阻塞点]
4. 等待用户确认继续
```

**标准收尾协议（pm 执行）：**
```
1. 更新 CHANGELOG.md（[Unreleased] 区块）
2. 更新 copilot-instructions.md → 当前迭代状态
3. 逐项核对 DoD Checklist
4. 如达到 Release 条件 → 触发 Release 流程
5. 本次会话变更摘要写入 docs/meetings/
```

### 决议 D — 新项目快速启动清单（Pre-flight Checklist）

任何项目使用本团队框架，brain 第一件事是逐条确认：

```markdown
## 新项目 Pre-flight Checklist

### 项目定位
- [ ] 项目类型（Web App / Library / CLI / Docs / Profile）
- [ ] 主要用户（个人 / 团队 / 开源社区）
- [ ] 技术栈（语言 + 框架 + 构建工具）
- [ ] 部署目标（GitHub Pages / Vercel / Docker / NPM）

### 仓库配置
- [ ] .github/copilot-instructions.md 已按项目定制
- [ ] .github/agents/ 已确认所需角色
- [ ] .editorconfig + .gitattributes 已创建
- [ ] CI（lint + test + build）第二个 commit 前就位
- [ ] CHANGELOG.md 已初始化（含 [Unreleased] 区块）

### 团队配置
- [ ] brain 理解项目目标和约束
- [ ] pm 已建立初始任务列表
- [ ] researcher 已完成技术选型预调研
- [ ] 首次 Sprint 计划已输出

### 个人信息 / 敏感信息
- [ ] 项目中的占位符已确认或替换
- [ ] 不公开的内容已确认边界
```

### 决议 E — 核心资产清单（带出去就能用）

| 资产 | 文件 | 描述 |
|------|------|------|
| 团队角色定义 | `.github/agents/brain.agent.md` ~ `code-reviewer.agent.md` | 五角色通用框架 |
| 项目全局指令 | `.github/copilot-instructions.md` | 项目大脑（替换个人信息即可用） |
| 团队手册 | `docs/team-playbook.md` | 会话协议 + 协作范式 + QA 流程 |
| 工作流说明 | `docs/agent-workflow.md` | 对外展示的 AI-Native 开发方式 |
| CI 模板 | `.github/workflows/link-check.yml` `markdown-lint.yml` | 开箱即用 |
| 工程配置 | `.editorconfig` `.gitattributes` `.markdownlint.jsonc` | 开箱即用 |
| Commit 规范 | `docs/team-playbook.md` § Commit 规范 | 语义化 + Release 时机 |

---

## 五、执行计划

| 任务 | 执行 | 优先级 |
|------|------|--------|
| 创建 `brain.agent.md` | dev | P0 |
| 创建 `pm.agent.md` | dev | P0 |
| 泛化 `content-writer` → `dev.agent.md` | dev | P0 |
| 泛化 `qa-reviewer` → `code-reviewer.agent.md` | dev | P0 |
| 新建 `docs/team-playbook.md`（核心资产手册） | dev | P0 |
| 更新 `copilot-instructions.md`（新团队结构 + DoD） | dev | P0 |
| 更新 `agent-workflow.md`（新协作模型） | dev | P0 |
| 保留 `profile-designer.agent.md`（项目专项） | - | P1 |
| 保留 `researcher.agent.md`（通用，无需修改） | - | ✅ |

---

## 六、行动项（立即执行）

> 本纪要存档后，dev 立即执行以下配置更新，无需等待用户确认。

- [ ] 创建 `brain.agent.md`
- [ ] 创建 `pm.agent.md`
- [ ] 更新 `content-writer.agent.md` → 泛化为 `dev`
- [ ] 更新 `qa-reviewer.agent.md` → 泛化为 `code-reviewer`
- [ ] 新建 `docs/team-playbook.md`
- [ ] 更新 `.github/copilot-instructions.md`
- [ ] 更新 `docs/agent-workflow.md`
- [ ] 更新 `CHANGELOG.md`

---

*会议结束。执行中。*
