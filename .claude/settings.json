{
  "hooks": {
    "TeammateIdle": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "你是 njueeRay Agent 团队的质量评审员。一个 Agent 成员即将进入空闲状态，请评估其工作是否达到 Done 标准。\n\n当前状态信息：$ARGUMENTS\n\n评估标准（任意一条未满足则返回 false）：\n1. 如果本次任务涉及代码/文档修改，是否有对应的 commit 消息（语义化格式：feat/fix/docs/style/chore）？\n2. 如果任务涉及新功能/重要变更，是否提及了 CHANGELOG.md 的更新（或明确说明不需要更新）？\n3. 如果任务是多步任务，是否所有声明的子任务都已完成？\n\n如果满足标准，返回：{\"ok\": true}\n如果不满足，返回：{\"ok\": false, \"reason\": \"具体说明哪条标准未满足，以及需要完成什么\"}\n\n注意：不要过于严苛。如果 Agent 明确说明了为什么跳过某项，或任务本身不涉及相关内容，可以通过。",
            "timeout": 30
          }
        ]
      }
    ],

    "TaskCompleted": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "你是 njueeRay Agent 团队的 DoD（Done of Definition）验证员。一个任务即将被标记为完成，请评估是否符合团队的完成标准。\n\n任务信息：$ARGUMENTS\n\nDoD 核心检查项（根据任务类型选择性评估）：\n- 文档类任务：相关 docs/ 文件是否已更新\n- 功能类任务：CHANGELOG.md 是否需要更新（未更新则应说明理由）\n- 会议/决策类任务：纪要是否已存档到 docs/meetings/\n- 设计决策类任务：是否记录在 design-decisions.md 或 copilot-instructions.md\n\n如果满足标准，返回：{\"ok\": true}\n如果不满足，返回：{\"ok\": false, \"reason\": \"具体说明缺什么，一句话\"}\n\n原则：这是质量保证，不是阻碍工作。如果 Agent 已经明确处理过相关内容，或解释了为什么不需要，都应通过。",
            "timeout": 30
          }
        ]
      }
    ],

    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\": {\"hookEventName\": \"SessionStart\", \"additionalContext\": \"=== njueeRay OpenProfile Project Context ===\\n项目状态：v5.5.0 已发布（成熟里程碑）\\n当前阶段：工具层升级 Sprint（Skills + Hooks + MCP）\\n核心原则：遵循 .github/copilot-instructions.md 中的铁律和设计选择\\n语言规范：中文优先，技术符号保留英文\\nBrain 是本会话的协调中枢，PM 负责任务跟踪，Dev 负责实现\"}}'",
            "async": false
          }
        ]
      }
    ],

    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "评估 Claude 是否可以停止工作。上下文：$ARGUMENTS\n\n检查：\n1. 用户的请求是否全部完成？\n2. 是否有明显遗漏的后续步骤（如 git commit 被提到但未执行）？\n\n如果可以停止，返回：{\"ok\": true}\n如果还有明显未完成的工作，返回：{\"ok\": false, \"reason\": \"具体说明还需要做什么\"}\n\n注意：不要因为「还可以做更多」就阻止停止——只有明确遗漏的必要步骤才应该阻止。",
            "timeout": 20
          }
        ]
      }
    ],

    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NonInteractive -File .claude\\hooks\\lint-markdown.ps1",
            "async": true
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "agent",
            "prompt": "检查刚刚写入的文件路径：$ARGUMENTS\n\n如果写入的是以下关键文件之一（满足任一条件即执行）：\n- copilot-instructions.md\n- design-decisions.md\n- team-playbook.md\n- evolution-events.jsonl\n\n请调用 Memory MCP（create_entities 或 add_observations）将文件中最新的关键决策、设计选择或重要变更摘要保存到知识图谱中。实体名格式：`OpenProfile:<file>:<YYYY-MM-DD>`。\n\n如果不是上述关键文件，直接返回，不做任何操作。",
            "async": true
          }
        ]
      }
    ]
  }
}
